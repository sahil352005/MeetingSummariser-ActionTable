# Streamlit Cloud Secrets Configuration
# Copy this file and add your actual API credentials

[api]
MISTRAL_API_URL = "https://your-mistral-endpoint.example/v1/generate"
MISTRAL_API_KEY = "sk-your-api-key-here"

# Instructions:
# 1. Replace the placeholder values above with your actual Mistral API credentials
# 2. Upload this file to your Streamlit Cloud app's secrets
# 3. The app will automatically use these credentials for AI-powered features
# 4. Without these secrets, the app will fallback to rule-based extraction
